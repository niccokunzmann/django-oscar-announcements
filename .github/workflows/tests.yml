# NOTE: This file is auto generated by OpenAPI Generator.
# URL: https://openapi-generator.tech
#
# ref: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: CI Tests

on: [push, pull_request]

permissions:
  contents: read

jobs:
  python-versions:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]

    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - name: Test the package
        run: |
          make test
      - name: Test the examples
        run: |
          cd example
          make test


  publish:
    runs-on: ubuntu-24.04
    environment:
      name: PyPI
      # see https://docs.pypi.org/trusted-publishers/using-a-publisher/
    permissions:
      id-token: write
    needs:
      - python-versions
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Set package version
        run: uv version ${{ github.ref_name }}

      - name: Build package
        run: uv build

      - name: Publish to PyPI
        run: uv publish

  deploy-github-release:
    # only deploy on tags, see https://stackoverflow.com/a/58478262/1320237
    if: startsWith(github.ref, 'refs/tags/v')
    needs:
    - publish
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
      - name: Create GitHub release from tag
        uses: ncipollo/release-action@v1
        with:
          allowUpdates: true
          body: "To view the changes, please see the [Changelog](https://niccokunzmann.github.io/python-royal-mail-click-and-drop-api/changelog/). This release can be installed from [PyPI](https://pypi.org/project/click_and_drop_api/#history)."
          generateReleaseNotes: false
